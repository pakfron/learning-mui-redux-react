import { Button, Typography } from '@mui/material'
import React from 'react'

export default function HomePage() {
  return (
    <div><Typography variant="h1" sx={{color:'red'}}>Hello World</Typography>
    <Button variant='contained'>Submit</Button>
    </div>
  )
}
